{
  "id": "03_command_scheduler",
  "title": "The Command Scheduler",
  "starterOpMode": "SchedulerOpMode.java",
  "starterFiles": ["SchedulerOpMode.java"],
  "steps": [
    {
      "title": "What the scheduler does every tick",
      "content": [
        {
          "type": "text",
          "value": "Each tick, the CommandScheduler loops through every active command.\n\nFor each one it calls execute().\n\nIf a command's isFinished() returns true, the scheduler calls end() and removes the command.\n\nThis is the heartbeat of command-based."
        }
      ]
    },
    {
      "title": "Command lifecycle",
      "content": [
        {
          "type": "text",
          "value": "Every command has four phases:\n\ninitialize() runs once when scheduled.\n\nexecute() runs every tick.\n\nisFinished() tells the scheduler when the command is done.\n\nend() runs once for cleanup.\n\nThe scheduler automatically walks each command through this lifecycle."
        }
      ]
    },
    {
      "title": "Scheduling a command",
      "content": [
        {
          "type": "text",
          "value": "There are two ways to schedule a command:\n\nschedule(new InstantCommand(() -> telemetry.addData(\"Status\", \"Running\")));\n\nOr call .schedule() directly on the command:\n\nnew InstantCommand(() -> telemetry.update()).schedule();\n\nBoth do the same thing. We prefer schedule() because it is cleaner."
        }
      ]
    }
  ]
}
