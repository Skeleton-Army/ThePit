{
  "id": "08_loop_times",
  "title": "Loop Times",
  "starterOpMode": "LoopTimesOpMode.java",
  "starterFiles": ["LoopTimesOpMode.java"],
  "steps": [
    {
      "title": "Why loop times matter",
      "content": [
        {
          "type": "text",
          "value": "The more often your control loop runs, the more responsive your robot is.\n\nA fast loop means: button presses register immediately, joystick movements feel smooth, and your PID controller updates frequently enough to stay accurate and avoid oscillation.\n\nA slow loop means: inputs feel laggy, driving is jerky, and your PID controller oscillates because corrections arrive too late.\n\nOn the simulator, the loop runs at a fixed ~20ms (50 Hz) because of the physics engine. On real hardware, loop times vary based on what your code does — and that's what this lesson is about."
        }
      ]
    },
    {
      "title": "What causes slow loops on real hardware?",
      "content": [
        {
          "type": "text",
          "value": "Most loop time is spent on hardware I/O — not your code.\n\nThere are two kinds of hardware I/O on an FTC robot:\n\nMotors — getCurrentPosition() and setPower() go over USB to the hub (or straight to the Control Hub's board). Motors are NOT I2C. Each read is still a round trip that costs time, but motors can be bulk read (see Fix 1).\n\nI2C devices — color sensors and distance sensors plug into the I2C bus. Each read costs several milliseconds, and unlike motors, they CANNOT be bulk read. A single color sensor read can take 5-10ms.\n\nSo a loop that reads 4 encoders, a color sensor, and a distance sensor might spend ~4ms on the motors plus ~15ms on the two I2C sensors before your PID even starts.\n\nHardware writes (setPower) also cost time, though writes can be batched.\n\nThis is not simulated — in the virtual robot, reads and writes are instant. But the code patterns you learn here apply directly to real hardware."
        }
      ]
    },
    {
      "title": "Measure loop time",
      "content": [
        {
          "type": "text",
          "value": "Your starter uses ElapsedTime to measure how long each loop iteration takes.\n\nRun the OpMode. The telemetry shows your loop time in milliseconds.\n\nIn the simulator, you'll see a steady ~20ms — that's the fixed physics engine cycle, not hardware I/O.\n\nOn a real robot, the same code would show variable times: maybe 22ms at idle, spiking to 40-50ms when reading many sensors. The goal is to bring that down as close to 20ms as possible."
        }
      ]
    },
    {
      "title": "Fix 1: Bulk reads",
      "content": [
        {
          "type": "text",
          "value": "The biggest loop time killer on real hardware is reading motor encoders one at a time.\n\nBy default, every getCurrentPosition() sends its own round trip to the hub. Four encoders = 4 round trips.\n\nBulk reading fixes this. Instead of sending one command per read, you send ONE command that fetches all the hub's cached data at once. Then each getCurrentPosition() just returns the already-fetched value — no wait.\n\nThe simplest way to enable it is using Marrow, which will be talked about in Module 5: Marrow.\n\nIn AUTO mode, the SDK automatically refreshes the bulk cache once per loop iteration. Every motor read after that just returns cached data — no manual clearBulkCache() calls needed. It's a one-time setup in init() that silently saves 20+ milliseconds per loop.\n\nImportant: bulk reads only help motors. I2C devices — color sensors and distance sensors — can't be bulk read. Each I2C read still costs its full several milliseconds, so keep the number of I2C reads in your loop as low as possible."
        }
      ]
    },
    {
      "title": "Fix 2: Cache motor writes",
      "content": [
        {
          "type": "text",
          "value": "Setting a motor to 0.5 power when it's already at 0.5 sends an unnecessary I2C write. Each write costs time.\n\nThe SDK automatically skips writes when the new power equals the current power. But what if it's very close — 0.5 vs 0.51? Same physical effect, but the SDK still sends a write.\n\nMotorEx solves this: its caching tolerance (default 0.0001) skips writes when the change is below the threshold. You can increase it with setCachingTolerance().\n\nThis means rapid PID corrections that nudge the power by 0.001 don't spam unnecessary hardware writes."
        }
      ]
    },
    {
      "title": "Summary",
      "content": [
        {
          "type": "text",
          "value": "To keep loop times fast:\n\nBulk read motor encoders at the top of loop() — one round trip to the hub for all of them instead of one per motor.\n\nUse MotorEx which caches writes — only sends setPower when the value meaningfully changes.\n\nMinimize I2C reads — color sensors and distance sensors can't be bulk read, so read each one only when you actually need it.\n\nRead sensor values once and store them in local variables — don't re-read the same encoder or I2C sensor twice.\n\nDon't call hardwareMap.get() inside loop() — that's for init() only.\n\nIn the simulator, these techniques don't change your measured loop time because there's no real I2C bus. But on a real robot with an Expansion Hub, they cut loop times from 40-50ms down to 2-5ms."
        }
      ]
    }
  ]
}
